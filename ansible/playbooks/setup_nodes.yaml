- hosts: kubernetes_control_planes:kubernetes_workers
  gather_facts: False

  tasks:
  - include_vars:
      file: ../vars.yaml

  - name: base
    import_role:
      name: base
    vars:
      kubernetes_version: "{{ kubernetes_version }}"
